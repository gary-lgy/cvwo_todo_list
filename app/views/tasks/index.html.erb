<div class="container-fluid">
  <div class="container-fluid">
    <%= render 'search_form' %>
  </div>
  <p>
    <%= render 'order_links', order_path: tasks_path %>
  </p>

  <%= render partial: 'task_list',
             locals: {
                 ongoing: @ongoing,
                 completed: @completed
             } %>
</div>

